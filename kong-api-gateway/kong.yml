_format_version: "3.0"
_transform: true

services:
  - name: bff-service
    url: http://bff-service:8000
    routes:
      - name: bff-route
        paths:
          - /        
        strip_path: false 

plugins:
  - name: rate-limiting
    config:
      second: 5
      hour: 1000